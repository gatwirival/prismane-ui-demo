import { createTheme } from '@prismane/core/themes';

const theme = createTheme({
  colors: {
    primary: {
      100: '#0070f3', // Replace with your desired color
    },
  },
});

export default theme;
